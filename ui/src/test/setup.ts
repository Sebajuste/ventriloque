// Les matchers de `jest-dom` (`toBeInTheDocument`, `toBeDisabled`...), charges une fois pour
// toute la suite. Voir `types` dans `tsconfig.json` pour leur declaration.
import "@testing-library/jest-dom/vitest";
